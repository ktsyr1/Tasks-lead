export type TaskOneType = {
    id?: number
    title?: string
    about?: string
    complete?: boolean
    timeComplete?: number
    totleTime?: number
    idGroup?: number
}
export type TaskGroupType = {
    id: number
    title: string
}